#include <iostream>
#include<cmath>
#include<algorithm>
#include <vector>
#define el <<"\n"
#define cel cout<<"\n"
#define ll long long
const ll maxn = 1e5 + 10;
int a[maxn];
using namespace std;
int main()
{
	int n;
	cin>>n;
	int arr[n],ar[n];
	for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n);
    cout<<arr[0]<<" "<<arr[1]<<" "<<arr[2] el;
}
