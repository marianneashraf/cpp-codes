#include <iostream>
using namespace std;
int main()
{
int k=505;
int n,q,x,arr[k];
for(int i=0;i<k;i++)
arr[i]=-1;
 cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>x;
        arr[x]=i;
    }
    int num;
    cin>>q;
    while(q--)
    {
        cin>>num;
        cout<<arr[num] el;
    }
}
